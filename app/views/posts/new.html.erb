<div class="new-form">
    <h3>新規投稿を作成</h3>
    <%= form_with(model: @post, local: true) do |form| %>
      <div><%= form.file_field :image %></div>
      <div><%= form.text_field :title, placeholder: "Title" %></div>
      <div><%= form.text_area :content, placeholder: "text" %></div>
      <div><%= form.submit "SEND" %></div>
    <% end %>
</div>